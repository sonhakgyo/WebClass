클래스 배열
    객체를 여러개 선언해야 하는 경우
    배열 타입으로 한 번에 선언 후 사용한다.

    클래스 배열은 2차원 배열이다.

클래스 배열 선언
    1. 클래스명[] 배열명 = new 클래스명[길이]; // 첫번째 방법이고, 자동 초기화된다.
    2. 클래스명[] 배열명 = {
            new 클래스명();
            new 클래스명();
       }

클래스 배열 사용
    배열명[index].변수명;
    배열명[index].메서드명();
















